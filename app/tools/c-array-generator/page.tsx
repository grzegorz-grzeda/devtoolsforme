import type { Metadata } from "next";import { CArrayGeneratorTool } from "@/components/c-array-generator-tool";import { ToolShell } from "@/components/tool-shell";import { createToolMetadata } from "@/lib/metadata";export const metadata: Metadata = createToolMetadata("C Array Generator", "Generate const uint8_t arrays from sample text for firmware source files.", "c-array-generator");export default function Page() { return <ToolShell slug="c-array-generator" eyebrow="Embedded" title="C Array Generator" description="Generate const uint8_t arrays from sample text for firmware source files."><CArrayGeneratorTool /></ToolShell>; }
