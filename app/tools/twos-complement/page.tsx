import type { Metadata } from "next";import { TwosComplementTool } from "@/components/twos-complement-tool";import { ToolShell } from "@/components/tool-shell";import { createToolMetadata } from "@/lib/metadata";export const metadata: Metadata = createToolMetadata("Two's Complement Converter", "Interpret integer values as signed, unsigned, binary, and hex across common widths.", "twos-complement");export default function Page() { return <ToolShell slug="twos-complement" eyebrow="Embedded" title="Two's Complement Converter" description="Interpret integer values as signed, unsigned, binary, and hex across common widths."><TwosComplementTool /></ToolShell>; }
