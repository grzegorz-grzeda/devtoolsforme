import type { Metadata } from "next";import { FixedPointConverterTool } from "@/components/fixed-point-converter-tool";import { ToolShell } from "@/components/tool-shell";import { createToolMetadata } from "@/lib/metadata";export const metadata: Metadata = createToolMetadata("Fixed-Point Converter", "Convert decimal values into scaled fixed-point integers using configurable Q formats.", "fixed-point-converter");export default function Page() { return <ToolShell slug="fixed-point-converter" eyebrow="Embedded" title="Fixed-Point Converter" description="Convert decimal values into scaled fixed-point integers using configurable Q formats."><FixedPointConverterTool /></ToolShell>; }
