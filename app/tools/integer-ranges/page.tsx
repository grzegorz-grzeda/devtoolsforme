import type { Metadata } from "next";import { IntegerRangesTool } from "@/components/integer-ranges-tool";import { ToolShell } from "@/components/tool-shell";import { createToolMetadata } from "@/lib/metadata";export const metadata: Metadata = createToolMetadata("Integer Type Ranges", "Reference min and max values for common fixed-width signed and unsigned C integer types.", "integer-ranges");export default function Page() { return <ToolShell slug="integer-ranges" eyebrow="Embedded" title="Integer Type Ranges" description="Reference min and max values for common fixed-width signed and unsigned C integer types."><IntegerRangesTool /></ToolShell>; }
