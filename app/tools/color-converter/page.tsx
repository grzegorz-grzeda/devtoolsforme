import type { Metadata } from "next";import { ColorConverterTool } from "@/components/color-converter-tool";import { ToolShell } from "@/components/tool-shell";import { createToolMetadata } from "@/lib/metadata";export const metadata: Metadata = createToolMetadata("Color Converter", "Convert hex colors into RGB and HSL with a live swatch preview.", "color-converter");export default function ColorConverterPage() { return <ToolShell slug="color-converter" eyebrow="Design" title="Color Converter" description="Convert hex colors into RGB and HSL with a live swatch preview."><ColorConverterTool /></ToolShell>; }
