import type { Metadata } from "next";import { ColorConverterTool } from "@/components/color-converter-tool";import { ToolShell } from "@/components/tool-shell";import { createToolMetadata } from "@/lib/metadata";export const metadata: Metadata = createToolMetadata("Color Converter", "Convert HEX, RGB, and HSL colors into richer design-ready formats with live previews and palettes.", "color-converter");export default function ColorConverterPage() { return <ToolShell slug="color-converter" eyebrow="Design" title="Color Converter" description="Convert HEX, RGB, and HSL colors into richer design-ready formats with live previews and palettes."><ColorConverterTool /></ToolShell>; }
