import type { Metadata } from "next";import { HTTPStatusTool } from "@/components/http-status-tool";import { ToolShell } from "@/components/tool-shell";import { createToolMetadata } from "@/lib/metadata";export const metadata: Metadata = createToolMetadata("HTTP Status Lookup", "Search common HTTP response codes and read what they mean in practice.", "http-status");export default function HTTPStatusPage() { return <ToolShell slug="http-status" eyebrow="Reference" title="HTTP Status Lookup" description="Search common HTTP response codes and read what they mean in practice."><HTTPStatusTool /></ToolShell>; }
