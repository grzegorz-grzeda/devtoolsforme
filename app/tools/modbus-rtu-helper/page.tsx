import type { Metadata } from "next"; import { ModbusRTUHelperTool } from "@/components/modbus-rtu-helper-tool"; import { ToolShell } from "@/components/tool-shell"; import { createToolMetadata } from "@/lib/metadata"; export const metadata: Metadata = createToolMetadata("Modbus RTU Helper", "Build Modbus RTU frames, append CRC bytes, and exchange them over a local serial port.", "modbus-rtu-helper"); export default function Page() { return <ToolShell slug="modbus-rtu-helper" eyebrow="Embedded" title="Modbus RTU Helper" description="Build Modbus RTU frames, append CRC bytes, and exchange them over a local serial port."><ModbusRTUHelperTool /></ToolShell>; }
