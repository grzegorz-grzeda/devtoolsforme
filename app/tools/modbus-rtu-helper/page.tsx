import type { Metadata } from "next";import { ModbusRTUHelperTool } from "@/components/modbus-rtu-helper-tool";import { ToolShell } from "@/components/tool-shell";import { createToolMetadata } from "@/lib/metadata";export const metadata: Metadata = createToolMetadata("Modbus RTU Helper", "Assemble simple Modbus RTU frames and append the correct CRC bytes.", "modbus-rtu-helper");export default function Page() { return <ToolShell slug="modbus-rtu-helper" eyebrow="Embedded" title="Modbus RTU Helper" description="Assemble simple Modbus RTU frames and append the correct CRC bytes."><ModbusRTUHelperTool /></ToolShell>; }
