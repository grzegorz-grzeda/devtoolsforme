import type { Metadata } from "next";import { MemoryViewerTool } from "@/components/memory-viewer-tool";import { ToolShell } from "@/components/tool-shell";import { createToolMetadata } from "@/lib/metadata";export const metadata: Metadata = createToolMetadata("Memory Viewer", "Inspect raw hex bytes as offsets, grouped hex, and printable ASCII.", "memory-viewer");export default function Page() { return <ToolShell slug="memory-viewer" eyebrow="Embedded" title="Memory Viewer" description="Inspect raw hex bytes as offsets, grouped hex, and printable ASCII."><MemoryViewerTool /></ToolShell>; }
